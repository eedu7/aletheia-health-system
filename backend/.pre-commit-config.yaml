repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.8
    hooks:
      - id: ruff-check
        args:
          - --fix
          - --extend-select=I
          - --select=F401
      - id: ruff-format